@page "/"
@using Hotelaria
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes, viewport-fit=cover" />
    <meta name="theme-color" content="#003580" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="format-detection" content="telephone=no" />
    <base href="~/" />
    <link href="css/site.css" rel="stylesheet" />
    <link href="css/mobile.css" rel="stylesheet" />
    <link href="Hotelaria.styles.css" rel="stylesheet" />
    <HeadOutlet />
</head>
<body>
    <component type="typeof(App)" render-mode="ServerPrerendered" />

    <div id="blazor-error-ui">
        <environment include="Staging,Production">
            Ocorreu um erro. Esta aplicação pode não responder até ser recarregada.
        </environment>
        <environment include="Development">
            Ocorreu um erro não tratado. Veja o console do navegador para detalhes.
        </environment>
        <a href="" class="reload">Recarregar</a>
        <a class="dismiss">🗙</a>
    </div>

    <!-- Carregar Blazor apenas UMA vez -->
    <script src="_framework/blazor.server.js" autostart="false"></script>
    
    <!-- Script de inicialização controlada -->
    <script src="js/blazor-init.js"></script>
    
    <!-- Scripts auxiliares (carregar DEPOIS do Blazor) -->
    <script src="js/session.js"></script>
    <script src="js/download.js"></script>
    <script src="js/mobile.js"></script>
</body>
</html>
