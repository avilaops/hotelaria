@inherits LayoutComponentBase
@inject AuthService AuthService
@inject NavigationManager NavigationManager
@implements IDisposable

<div class="page">
    <div class="sidebar">
        <div class="top-row ps-3 navbar navbar-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="">🏨 Hotelaria</a>
            </div>
        </div>

        <nav class="flex-column">
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                    <span class="icon">🏠</span> 
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="reservas">
                    <span class="icon">📋</span> 
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="hospedes">
                    <span class="icon">👥</span> 
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="disponibilidade">
                    <span class="icon">📅</span> 
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="financeiro">
                    <span class="icon">💰</span> 
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="relatorios">
                    <span class="icon">📊</span> 
                </NavLink>
            </div>
        </nav>

        <!-- Botão de Configuração no Rodapé -->
        <div class="sidebar-footer">
            <NavLink class="config-button" href="configuracao">
                <span class="icon">⚙️</span>
                <span class="text">Configurações</span>
            </NavLink>
        </div>
    </div>

    <main>
        <div class="top-row px-4">
            @{
                var usuario = AuthService.ObterUsuarioAtual();
            }
            <div class="user-menu">
                <span class="user-info">
                    <span class="icon">👤</span> 
                    <span class="user-name">@usuario?.Nome</span>
                    <span class="user-perfil">(@AuthService.ObterNomePerfil(usuario?.Perfil ?? PerfilUsuario.Visualizador))</span>
                </span>
                <button class="btn btn-secondary btn-sm" @onclick="RealizarLogout">
                    🚪 Sair
                </button>
            </div>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

@code {
    protected override void OnInitialized()
    {
        AuthService.OnAuthStateChanged += StateHasChanged;
    }

    private void RealizarLogout()
    {
        AuthService.Logout();
        NavigationManager.NavigateTo("/login", forceLoad: true);
    }

    public void Dispose()
    {
        AuthService.OnAuthStateChanged -= StateHasChanged;
    }
}
